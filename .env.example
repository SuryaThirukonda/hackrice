# The agent service (npm run agent) reads the repo root .env first, then this folder's .env.
OPENAI_KEY=sk-...
# Model for Fight Night agents. Falls back to gpt-5-mini automatically if this id is rejected.
AGENT_MODEL=gpt-5.6-luna
AGENT_PORT=8790
PRESSAGE_KEY=
ELEVENLABS_KEY=