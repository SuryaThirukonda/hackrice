{
  "name": "hap-edge",
  "private": true,
  "type": "module",
  "scripts": {
    "build:web": "cd ../web && npm run build",
    "dev": "wrangler dev",
    "deploy": "npm run build:web && wrangler deploy",
    "set-backend": "node scripts/set-backend.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.131.1"
  },
  "dependencies": {
    "@cloudflare/workerd-linux-64": "^1.20260911.1"
  }
}
